# 仓库根依赖入口 — 见 docs/依赖说明.md §4
#
# Web / Docker 运行时     → 本文件（等同 requirements-web.txt）
# Web 后端开发 + 测试工具 → pip install -r requirements-dev.txt
# 桌面 GUI / 终末地开发   → cd games/endfield && pip install -e ".[dev]"
-r requirements-web.txt
