# 代码结构规范

> **约束来源**：[ADR-0001：代码目录与文件规模约束](adr/0001-code-layout-constraints.md)  
> **受众**：维护者、贡献者、Cursor Agent（新代码与重构均须遵守）

---

## 1. 两条铁律

### 1.1 目录宽度

- 任意目录下 **直接子项（文件 + 文件夹）≤ 10**。
- **数子项时包括**：`.py`、子文件夹、`README.md` 等；**不包括** `__pycache__`、`.pytest_cache`。
- 第 11 个子项出现前，必须先建子目录并迁入，或合并相关文件。

### 1.2 文件长度

- **目标**：单个 `.py` **≤ 400 行**（可读、可测、可 review）。
- **硬顶**：业务/GUI/计算代码 **> 500 行不得新增**；已有超长文件列入迁移 backlog，拆分时优先动刀。
- **例外申请**：须在 PR 或 Issue 说明理由，并更新 ADR 或本文件 §5 豁免表。

---

## 2. 放什么、不放什么

| 位置 | 放 | 不放 |
|------|----|------|
| `adapters/endfield/calc/` | 伤害链、搜索、配装优化、与 GUI 无关的纯逻辑 | CTk/PySide6 控件、文件对话框 |
| `adapters/endfield/data_loading/` | loader、facade、catalog | CV/PySide6/展示逻辑 |
| `games/endfield/gui_design/` | CTk/PySide6 布局、事件、展示；按 `presentation` / `app` / `panels` / `controls` / `shell` 分子包 | 直接读 JSON 数据（走 `adapters/endfield/data_loading/`） |
| `tools/` | CLI 脚本、设计器页签、OCR、爬虫（不含入口 .py，入口统一放根 `main_*.py`） | 框架核心逻辑 |
| `resources/` | 业务资源文件（如捐赠二维码 `donation/donation_qr.png`）；打包时用 `--add-data` 包含 | 代码模块、用户数据 |
| 根 `main_*.py` | 5 个入口：`main.py`（计算器）/ `main_designer.py`（数据设计器）/ `main_pack_designer.py`（配置包设计器）/ `main_launcher.py`（启动器）/ `main_build.py`（打包） | 包内 import 的模块 |
| `tests/` | 与生产**同构**的子目录 + `conftest.py` / 共享 fixture | 与生产无关的「杂物测试」堆根目录 |
| `scripts/` | 录入、seed、本地 CLI | 被 GUI 导入的运行时代码 |
| `tools/`（仓库根） | BWIKI、上传、审计 | 包内 import 的模块 |
| **Web 后端** | `web/backend/api/` | FastAPI 路由模块（compute / adapters / data / layout / search） |
| **Web 前端页面** | `web/frontend/src/pages/` | 页面级 React 组件（Compute / Designer / PackDesigner / Adapters / Editor） |
| **Web 前端组件** | `web/frontend/src/components/` | 按三个 GUI 分子目录（calculator / designer / pack_designer / compute） |
| **Web 前端 Store** | `web/frontend/src/store/` | Zustand 状态管理，按页面对应命名（computeStore / designerStore / packDesignerStore） |
| **Web 前端 API** | `web/frontend/src/api/` | TypeScript API 调用封装，与 `web/backend/api/` 一一对应 |
| **Web 版本号** | `web/package.json` `version` | 单独维护，与 `_VERSION` / `_EXE_VERSION` 解耦 |

---

## 3. 命名与拆分手法

1. **先按领域拆目录**，再按职责拆文件（与「一个文件夹里堆 20 个 utils」相反）。
2. 单类过大：用 **mixin** 或 **按 UI 区块拆模块**（本项目 `gui_design/shell/app_*.py` 为范例）。
3. 纯函数集合：独立 `*_lines.py` / `*_helpers.py`，由薄 `__init__.py` 或聚合模块 re-export。
4. **Import（方案 B）**：根路径 stub 已移除；**新代码禁止** `from gui_design.loadout_state` 等旧路径。
5. 注释语言：**中文**（与仓库约定一致）。
6. **拆分工具**：优先 `python tools/split_module_by_symbols.py --only soft|core`（AST 符号切分）；**禁止**行号硬切。
7. **GUI 字体规范**：所有 CTk/PySide6 控件（`CTkButton`/`CTkLabel`/`CTkEntry`/`CTkOptionMenu`/`CTkCheckBox`/`CTkSwitch`/`CTkComboBox`/`CTkTextbox`）必须在构造时显式传入 `font=` 参数。字体通过以下途径获取：
   - 主应用优先 `self.small_font` / `self.big_font`（`gui_fonts.default_ui_font()` 创建，族名与系统一致）
   - 独立模块用 `default_ui_font(size=...)` 直接调用
   - 全局兜底：`gui_settings.configure_tk_default_font(root)` 在根窗口创建后覆写 `TkDefaultFont` **和** `ThemeManager.theme["CTkFont"]["family"]`（后者是 CTkOptionMenu/CTkComboBox 下拉菜单 DropdownMenu 动态创建 `CTkFont()` 的默认族名来源）；`gui_settings.configure_all_tk_fonts(root)` 在 widget 构建完毕后遍历全部 tk 命名字体二次兜底
   - 详见 [utils/gui_fonts.py](../endfield_damage_calculator/utils/gui_fonts.py) 和 [gui_design/shared/gui_settings.py](../endfield_damage_calculator/gui_design/shared/gui_settings.py)
8. **类型安全 — 模块级导入**：如果类型被**静态方法**（`@staticmethod`）、**类方法**（`@classmethod`）或**类作用域**（如类型注解 `-> list[SomeType]`）引用，必须在**模块顶部**用 `from ... import SomeType` 显式导入。禁止将这些类型放在方法内部的「惰性导入」（`from ... import SomeType`）中，因为静态方法/类作用域不在执行期作用域链上，Pylance 会报「未定义」错误。
   - ✅ 正确：`from utils.gui_help_dialog import HelpSection`（顶部）→ `@staticmethod def build() -> list[HelpSection]:`
   - ❌ 错误：`def show(self): from utils.gui_help_dialog import HelpSection; ...` 然后 `@staticmethod` 使用 `HelpSection`
9. **Qt 枚举值 — 禁止字面量整数**：所有 Qt 枚举参数必须使用枚举成员，**禁止**使用裸整数或 `Literal` 类型传参。Pylance 的 Qt 绑定会严格检查枚举类型，字面量整数会报「无法分配」类型错误。
   - ✅ 正确：`scroll.setFrameShape(QFrame.Shape.NoFrame)`、`self.tabs.setTabPosition(QTabWidget.TabPosition.North)`
   - ❌ 错误：`scroll.setFrameShape(0)`、`setTabPosition(1)`
10. **惰性导入（lazy import）规则**：
    - ✅ 惰性导入只用于：运行时才确定的模块（如插件、可选依赖）、递归导入避免
    - ❌ 惰性导入不用于：类型定义、基类、`@staticmethod`/`@classmethod` 使用的符号
    - 惰性导入时，`from` 中只放该模块中该函数**执行时**才需要的符号（如从 `from utils.gui_help_dialog import HelpDialog` 而非 `... import HelpDialog, HelpSection`）

11. **新增依赖许可检查**：每次向 `pyproject.toml` 或 `requirements.txt` 添加新的第三方库时，必须先确认其许可证并更新文档：

    - 从 PyPI 或官方仓库获取 SPDX 许可证标识
    - **禁止引入**（除非律师审阅）：
      - GPL-3.0-only / GPL-3.0-or-later（传染性过强）
      - SSPL（非开源，商业受限）
      - 无许可证或自定义许可证（权利不明）
    - **谨慎使用**：
      - AGPL-3.0：项目自身许可；可选依赖禁止引入 AGPL，须替换为宽松许可
      - LGPL：可接受（如 PySide6），须保留版权声明，动态链接
    - ✅ **安全新增**：MIT、Apache 2.0、BSD-2/3-Clause、ISC、PSF License、HPND
    - 更新 [`NOTICES.md`](../NOTICES.md) 软件依赖表
    - 更新 [`docs/数据来源与许可.md`](数据来源与许可.md) §十四
    - 特殊条款（如 YOLO 模型商用受限）须在文档中注明

---

## 4. 当前基线（2026-05-29）

### 4.1 目录宽度

| 目录 | 状态 |
|------|------|
| `tests/` | ✅ ≤10 子项；镜像 `calculation/`、`gui_design/` 等 |
| `gui_design/` 根 | ✅ 9 子包 |
| `calculation/` 根 | ✅ 10 子项 |
| `gui_design/controls/` | ✅ 已收为 `enhancement/`、`search/`、`multi_skill/` |
| `calculation/search/` | ✅ `plan/`、`run/`、`evaluate/`、`persist/` |
| `web/backend/` | ✅ 根 2 子项（`main.py`、`api/`） |
| `web/backend/api/` | ✅ 当前 2 文件（`compute.py`、`adapters.py`）；新增路由模块时 ≤10 |
| `web/frontend/src/pages/` | ✅ 当前 3 文件；计划新增 2 个（DesignerPage + PackDesignerPage） |
| `web/frontend/src/components/` | ✅ 当前 1 子目录（`compute/`）；新增时按 GUI 分子目录 |
| `web/frontend/src/store/` | ✅ 当前 3 文件；计划新增 2 个 |

### 4.2 文件长度（包内业务代码）

| 状态 | 说明 |
|------|------|
| ✅ | 业务/GUI/计算 `.py` 均 **≤500 行硬顶**（2026-05-29 门禁：0 ERROR，1 WARN = `qt_app.py` 461 行） |
| 待拆 | `scripts/seed_weapons.py`（~1451 行，数据 seed；`scripts/seed_*.py` 在门禁中豁免行数检查） |

门禁命令（仓库根）：

```powershell
python tools/check_layout.py --max-lines 400
```

日常 CI/上传前可用 `--warn-only-lines` 仅硬顶（500 行 + 目录宽度）阻断。

---

## 5. 豁免表

| 路径 | 原因 | 复审日期 |
|------|------|----------|
| `scripts/seed_*.py` | 数据表 + 录入脚本；逻辑与数据块分离待后续 | 2026-06 |

---

## 6. Agent / PR 自检清单

提交前（或 Agent 完成大块后）：

- [ ] 未新增「目录直接子项 > 10」
- [ ] 未新增「业务 .py > 400 行」（硬顶 500）
- [ ] 新模块 import 走**新路径**，未增加 stub 依赖
- [ ] 测试路径与生产结构对齐；patch 打**实现模块**
- [ ] 较大结构变更已更新 [`会话接续手册.md`](会话接续手册.md) §3–§4
- [ ] Qt 枚举值已使用枚举成员，未使用字面量整数
- [ ] 静态方法/类方法使用的类型已在模块顶部导入，未藏在惰性导入中
- [ ] 仓库根跑过 `python tools/check_layout.py --max-lines 400`
- [ ] **Web 前端**：TypeScript 类型完整（无隐式 `any`）；React 组件不遗漏 key props
- [ ] **Web 后端**：FastAPI route 函数有完整类型注解；Pydantic model 定义在 route 旁
- [ ] **同步检查**：`layout.json`/`attr_schema.json` 变更后，确认 PySide6 ComputeSheet 和 Web ComputeSheet 渲染一致
- [ ] **目录约束**：未在 `web/frontend/src/components/` 根平铺大量组件（应按三个 GUI 分子目录）

---

## 7. 相关文档

- [ADR-0001](adr/0001-code-layout-constraints.md)
- [会话接续手册 §4.25–§4.26](会话接续手册.md)
- [`tools/split_module_by_symbols.py`](../tools/split_module_by_symbols.py)
- [`tools/check_layout.py`](../tools/check_layout.py)
