# 框架对齐分析

> 创建：2026-06-02 | 来源：代码规范与架构审查

## 概述

本文记录两个生产级游戏适配器（终末地 / 明日方舟）与通用计算框架（`calc-framework`）的对齐状态，以及框架本身的功能缺口。

---

## 1. 明日方舟 — 基本对齐

| 维度 | 状态 | 说明 |
|------|:----:|------|
| `framework_bridge.py` | ✅ | 存在且正确导出 4 个框架组件 |
| GUI 不直接导入 `calc_framework` | ✅ | `gui/` 下 0 处 `from calc_framework` |
| ComputeSheet + `layout.json` | ✅ | 5 个 section，声明式渲染，`user_context_overrides` 映射完整 |
| 无自研计算引擎 | ✅ | 纯 DAG 架构，通过 `AdapterPackage.dag_service.evaluate()` |
| DAG 定义在适配器目录 | ✅ | `framework/adapters/arknights/dag/arknights_full.dag.json` |
| 适配器元数据完整 | ✅ | `meta.json` 含所有必要声明 |
| 包目录有 `__init__.py` | ✅ | 所有子包规范 |

### 小问题
- `ArknightsApp.py:46` 引用了不存在的 `games.arknights.data_loading.loader` ✅ 已修复（2026-06-02）
- Web API（`web/backend/api/arknights.py`）直接 `from calc_framework`（但 Web 层规范未要求走桥接）

---

## 2. 终末地 — 部分对齐

| 维度 | 状态 | 说明 |
|------|:----:|------|
| `framework_bridge.py` | ⚠️ 很薄 | 只导入了 5 个符号；`calc/` 下 14 处框架全部绕过 |
| ComputeSheet + `layout.json` | ✅ | 13 个 section，覆盖全部 15 乘区 |
| `calc/dag_adapter/` | ✅ | 结构完整，DAG ↔ zone_snapshot 双向桥接 |
| 双引擎 | ❌ **存在** | 本地引擎仍主导搜索/优化/手动 Buff；DAG 引擎仅覆盖 zone_snapshot |
| 框架搜索接口 | ⚠️ 委托模式 | `EndfieldSearchEngine` 实现 `SearchEngine` ABC，内部仍调本地引擎 |

### 双引擎范围对比

| 功能 | 本地引擎 | DAG 引擎 |
|------|:--------:|:--------:|
| zone_snapshot 展示 | — | ✅ |
| ComputeSheet 乘区展示 | — | ✅ |
| 全量搜索/配装优化 | ✅ | — |
| 手动 Buff（物理/法术） | ✅ | — |
| 处决/受击伤害 | ✅ | — |
| 生存估算 | ✅ | — |

两引擎数值一致性已由 `test_formula_consistency.py` 验证（偏差 < 0.001）。

---

## 3. 框架缺口

| # | 严重度 | 问题 | 文件 | 状态 |
|:-:|:------:|------|------|:----:|
| 1 | 🔴 严重 | **Launcher 损坏**：`ComputeSheet(pkg.dag_service)` 缺 `layout` 和 `variables` 参数 | `framework/src/calc_framework/launcher.py:128` | ✅ 已修复 |
| 2 | 🟡 中等 | **适配器模板文件缺失**：`framework/adapters/_template/` 下 `meta.json`、`functions.py`、`attr_schema.json` 不存在 | `framework/adapters/_template/` | ✅ 已修复（含 `discover_adapters` 跳过 `_` 前缀目录） |
| 3 | 🟡 中等 | **模板未覆盖 `user_context_overrides`**：两个游戏都依赖此机制，模板 GUI 完全未提及 | `docs/game-template/games/_template/gui/` | ✅ 已修复（含 `framework_bridge.py` 补充 `ComputeSheet`/`load_layout_json` 导出） |
| 4 | 🟢 轻微 | **终末地 `entry_dag` 路径异常**：指向 `../../src/calc_framework/configs/`，跨出适配器目录 | `framework/adapters/endfield/meta.json` | ✅ 已修复 |

---

## 4. 当前基线

- 框架核心（`AdapterPackage`、`AdapterManager`、`ComputeSheet`、`DAGService`）：**成熟稳定**
- 明日方舟：**已对齐**，可作为新游戏适配的参考实现
- 终末地：**部分对齐**，本地引擎 → DAG 迁移是已知的长期目标（P2）
- 三个演示适配器（card_rpg / fps / moba）：仅验证跨品类能力，无 GUI
