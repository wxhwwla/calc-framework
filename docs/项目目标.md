# 项目目标

> 每次对话开始前，必须完整阅读本文档，了解项目的长远愿景和当前焦点。

**最后更新**：2026-05-30

---

## 1. 长远目标

将项目打造为一套**完整的抽象框架**，可以接入任何以数值驱动的游戏，实现「一套框架，万物可算」。

### 1.1 核心理念

框架按三层设计流程构建：

```
第一层：乘区层（计算引擎）
  ├── DAG 图编辑器 — 可视化编辑所有乘区所需的计算逻辑
  ├── 复合节点封装 — 将细粒度 DAG 节点组合为可复用的复合节点
  ├── 封装为块（Block） — 复合节点进一步封装为逻辑块（如属性块、攻击力块）
  ├── 遍历逻辑 — 配装搜索/枚举框架
  └── 数据接入 — 编辑器读取已有数据（爬虫/手动录入/OCR 等），辅助验证乘区是否正确

第二层：布局层（UI 设计器）
  ├── 可视化布局编辑器 — 类似 Qt 的拖拽式控件放置（选项框、下拉框等）
  ├── 乘区映射 — 将第一层的乘区变量映射到 UI 控件中
  └── 布局导出 — 输出一个布局定义文件

第三层：展示层（用户框架）
  ├── 加载布局文件 + 数据文件
  └── 供终端用户直接使用的计算界面
```

### 1.2 目标形态

| 维度 | 目标 |
|------|------|
| 受众 | C 端全体玩家 + B 端游戏从业者（数值策划、独立开发者、攻略作者） |
| 品类 | 端游/手游/单机/网游/MOBA/FPS/回合制/战棋 — 任意数值驱动游戏 |
| 内核 | 纯数学引擎 + 动态规则配置，与具体游戏解耦 |
| 扩展 | 数据接入（爬虫/手动/OCR 等多来源）、插件工坊（Calc Hub）、社区共享配置 |
| 商业模式 | GPL 开源内核 + 商业双授权（可选） |

---

## 2. 近期目标

按三层设计流程梳理当前**已完成**与**待完成**的部分。

### 2.1 第一层：乘区层（计算引擎）— 完成度 ~80%

#### ✅ 已完成

| 模块 | 状态 | 说明 |
|------|------|------|
| DAG 引擎 | **完成** | 9 种节点类型、拓扑排序、AST 沙箱安全求值、子图展开 — 工业级 |
| 图编辑器（Graph Editor） | **完成** | QGraphicsView 可视化编辑画布、节点拖拽连线、网格吸附、复合节点双击进入子图 |
| 终末地 15 乘区完整 DAG | **完成** | `endfield_full.dag.json` 完整配置 |
| 自定义函数注册 | **完成** | `sandbox.register_function()` + 适配器自动加载 |
| 搜索/枚举引擎 | **完成** | Top-N 追踪、并行执行器、取消令牌 |
| 终末地搜索业务层 | **完成** | 固定配装、多技能加权、MVP 续跑、SQLite 持久化 |
| 插件系统 | **完成** | 基类 + 注册表 + 3 内置插件（暴击/闪避/距离衰减） |
| 日志系统 | **完成** | `calc_framework/logging.py` 统一日志 |

#### 🟡 待增强

| 模块 | 状态 | 说明 |
|------|------|------|
| 块级封装（Block） | **已设计未实现** | ADR-0011 定义了 6 块划分，但块级缓存、块级跳过求值未实现 |
| 跨品类验证 | **待验证** | 有 card_rpg/fps/moba 示例适配器，但未真正跑通验证 |

### 2.2 第二层：布局层（UI 设计器 / 开发者工具）— 完成度 ~55%

**这是当前的攻坚重点** — 三页签框架（数据/布局/主题）已建立，但核心数据链路未通。

#### ✅ 已完成

| 模块 | 状态 | 说明 |
|------|------|------|
| ComputeSheet | **完成** | 声明式计算表，根据 layout.json + DAG variables 自动渲染控件 |
| 布局定义格式 | **完成** | `layout.json` schema（ui-v1），Section/columns 定义 |
| 布局编辑器 GUI | **完成** | 可视化编排 DAG 变量到 layout.json Section |
| 控件自动推断 | **完成** | `infer_control()` 根据变量类型自动选择控件 |
| 数值格式化 | **完成** | `format.py` 数值展示格式化 |
| `.calcpack` 导出器 | **完成** | ZIP 打包函数就绪 |

#### 🟡 待完成

| 模块 | 状态 | 说明 |
|------|------|------|
| 数据编辑器与 `data_pipeline` 打通 | **待实现** | 核心链路：data_pipeline 标准数据 → DataEditorPanel 字段级展示/编辑 |
| 布局编辑器升级（拖拽式） | **基本完成** | 网格吸附、碰撞检测已实现，缺真正的拖拽控件放置 |
| 三页签数据共享 | **待实现** | 数据编辑器的数据无法传入布局编辑器和导出器 |
| 多主题实时切换 | **待实现** | theme.json 加载时读取，运行时不可换肤 |

### 2.3 第三层：展示层（用户框架）— 完成度 ~40%

#### ✅ 已完成（终末地专用）

| 模块 | 状态 | 说明 |
|------|------|------|
| 终末地 GUI | **完成** | 完整的双页签计算器 + 搜索 + 仪表盘 |
| `.calcpack` 打包格式定义 | **完成** | ADR-0006 定义了 ZIP 包格式 |

#### ❌ 未开始（通用版本）

| 模块 | 状态 | 说明 |
|------|------|------|
| 通用用户框架 | **0%** | 能从 .calcpack + 数据文件动态渲染的通用展示层 |
| 多游戏切换 UI | **0%** | 无游戏选择器、无适配包管理页面 |
| Calc Hub / 插件工坊 | **0%** | 社区分享平台，纯概念阶段 |

---

## 3. 数据与编辑器设计理念

### 3.1 数据结构定位

已有 `tools/bwiki_scout/` 爬虫爬取了终末地的角色/武器/装备 JSON 数据。这些数据经过 `tools/data_pipeline/` 的 ETL 清洗后，形成标准四层 schema（EntitySchema / SkillSchema / SegmentSchema）。

**这些标准格式的数据就是模板**：
- 后续任何游戏的清洗流程都按这套结构来
- 开发者工具（`tools/designer/`）直接读取这些数据
- 用户可以在数据编辑器中看到已有数据，并验证乘区计算是否与游戏数据吻合

### 3.2 数据接入方式（并列可选，非必须）

| 方式 | 状态 | 说明 |
|------|------|------|
| **爬虫/脚本**（BWIKI scout） | ✅ 已有 | `tools/bwiki_scout/` 已爬取终末地全量数据 |
| **手动录入** | ✅ 已有 | `tools/data_pipeline/` CSV/JSON → 标准 schema |
| **手动 JSON 编辑** | ✅ 已有 | 直接编辑 `characters.json` / `weapons.json` |
| **OCR 截图识别** | ❌ 未开发 | 未来可选补充，不是当前焦点 |

OCR **不是必须的** — 它只是数据接入的选项之一，与爬虫、手动录入并列。

---

## 4. 路线图

### 4.1 完整链路现状

```
                      ┌──────────────────────┐
                      │   终末地旧 JSON 数据   │
                      │  characters.json 等   │
                      └──────────┬───────────┘
                                 │
                      ┌──────────▼───────────┐
                      │  tools.data_pipeline  │  ✅ 有 ETL + 标准 schema
                      │  四层 EntitySchema    │
                      └──────────┬───────────┘
                                 │  ↓ from_legacy_endfield
                      ┌──────────▼───────────┐
         ┌───────────│  标准格式数据（模板）    │───────────┐
         │           └──────────────────────┘           │
         │  ❌ 未打通                                  │  ❌ 未打通
         ▼                                              ▼
  ┌──────────────┐                            ┌──────────────────┐
  │ tools/       │                            │ framework/       │
  │ designer/    │                            │ ComputeSheet     │
  │ (骨架)        │                            │ (终末地版)       │
  └──────────────┘                            └──────────────────┘
```

### 4.2 现状图谱

| 层 | 完成度 | 说明 |
|----|--------|------|
| 乘区层（计算引擎） | ~80% | 非常成熟，只差块级封装 |
| 布局层（设计器） | ~55% | **骨架在，核心链路没通** |
| 展示层（用户框架） | ~40% | 终末地 GUI 完成，通用版 0% |

**核心堵点**：data_pipeline 标准数据 ↔ tools/designer/ 之间的链路是断裂的。

### 4.3 优先级分解

#### P0（当前焦点）：打通 data_pipeline → tools/designer 数据链路

| 步骤 | 任务 | 说明 |
|------|------|------|
| P0-1 | 将终末地数据迁移为标准格式 | 运行 `data_pipeline.cli` 迁移 CLI，生成 `characters_standard.json` / `weapons_standard.json` |
| P0-2 | 增强 DataEditorPanel | 按实体类型（角色/武器/装备）分 tab 展示，字段级查看/编辑（名称、星级、技能列表、段倍率等），不再是纯 JSON 文本编辑 |
| P0-3 | 自动加载终末地标准数据 | DataEditorPanel 启动时自动加载标准格式的终末地数据作为模板 |
| P0-4 | 数据 → DAG 验证 | 读取数据后，能将角色属性注入 DAG 引擎求值，验证乘区计算是否与游戏数据吻合 |

#### P0.5：完整设计-打包链路

| 步骤 | 任务 | 说明 |
|------|------|------|
| P0.5-1 | 布局编辑器升级为拖拽式 | 真正的拖拽控件放置、乘区变量映射 |
| P0.5-2 | 三页签数据共享 | 数据编辑器 → 布局编辑器 → 导出器 数据流通 |
| P0.5-3 | 导出链路验证 | `data + DAG + layout → .calcpack` 完整链路跑通 |

#### P1：通用展示层

| 步骤 | 任务 |
|------|------|
| P1-1 | ComputeSheet 升级为通用用户框架，加载 `.calcpack` 动态渲染 |

#### P2：块级封装 + 跨品类验证 + 扩展

| 步骤 | 任务 |
|------|------|
| P2-1 | 终末地 15 乘区重构为 6 块，块级缓存 |
| P2-2 | 跑通 card_rpg/fps/moba 适配器 |
| P2-3 | 响应式布局 + 多主题实时切换 |
| P2-4 | Calc Hub / 插件工坊 |
| P2-5 | OCR 截图识别（可选） |

---

## 5. 三层设计愿景对照

| 用户描述的流程 | 对应模块 | 完成度 |
|--------------|---------|--------|
| ① DAG 图编辑所有乘区计算逻辑 | `dag/engine.py` + `graph_editor/` | ✅ 完成 |
| ② 封装成复合节点 | `dag/subgraph.py`（call + subgraph） | ✅ 完成 |
| ③ 封装成块 | ADR-0011 块划分 | 🟡 已设计未实现 |
| ④ 遍历逻辑 | `search/engine.py` + `search/parallel.py` | ✅ 完成 |
| ⑤ 数据接入（爬虫/OCR 等 → 编辑器） | 爬虫 ✅ 已有；**编辑器读取数据** 待实现 |
| ⑥ 可视化布局编辑 | `editor/gui.py` + `layout.py` | ✅ 完成 |
| ⑦ 乘区映射到 UI 控件 | `ComputeSheet` + `layout.json` | ✅ 完成 |
| ⑧ 输出文件（.calcpack） | `.calcpack` 打包 | 🟡 已设计未完全实现 |
| ⑨ 用户展示框架 | `ComputeSheet`（通用版） | 🟡 终末地版完成，通用版未开始 |
