---
description: 新对话必须先读会话接续手册，再改本仓库代码
alwaysApply: true
---

# 会话接续（本仓库）

在本仓库（`endfield_damage_calculator`）中开始任何实现、调试、发布、BWIKI 或架构相关任务之前：

1. **先读** [`docs/会话接续手册.md`](docs/会话接续手册.md)（用户 @ 该文件时同样必须先读）。
2. 再按需读 [`AGENTS.md`](AGENTS.md)、[`CONTEXT.md`](CONTEXT.md)、[`docs/操作指令集.md`](docs/操作指令集.md)；**上传或 pre-commit 排障**时再读 [`docs/上传脚本与-pre-commit.md`](docs/上传脚本与-pre-commit.md)。
3. 勿重复已完成工作（见手册 §3）；术语与接缝以手册 §2 为准。
4. **GitHub 上传由人类执行**：默认不要跑 `github_upload_module.py`；就绪后提示用户在 `[根]` 自行上传。仅当用户当次明确要求时才代跑；禁止裸 `git push`。
5. 完成较大功能块后，提醒用户更新手册 §3、§4 与文首日期（上传仍由用户自己做）。

若用户仅问与代码库无关的一般性问题，可跳过手册。
