# 游戏包 CI — 终末地伤害计算器
# 包路径：adapters/endfield/，测试在此目录下

name: Game CI

on:
  push:
    branches: [main, master]
    paths:
      - "games/endfield/**"
      - "adapters/endfield/**"
      - ".github/workflows/ci.yml"
  pull_request:
    branches: [main, master]
    paths:
      - "games/endfield/**"
      - "adapters/endfield/**"

jobs:
  test:
    runs-on: windows-latest
    defaults:
      run:
        working-directory: .
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install framework (editable)
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e framework
      - name: Install game package with dev deps
        run: |
          python -m pip install -e games/endfield ".[dev]"
      - name: Run tests
        run: |
          python -m pytest adapters/endfield/tests/ -q --cov=adapters.endfield.calculation --cov=adapters.endfield.data_loading --cov=adapters.endfield.tests.gui_design --cov-report=term-missing --cov-report=xml
      - name: Coverage summary
        run: python -m coverage report --fail-under=40
